version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: docker/development/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - /var/www/portfolio/node_modules
      - .:/var/www/portfolio
    environment:
      - CHOKIDAR_USEPOLLING=true
